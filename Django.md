# Django notebook

### directory

```python
mysite # is just a container
├── manage.py # a command-line utility
└── mysite # is the actual python package for my project
    ├── __init__.py # an empty file that tells pyton that this directory should be considered a python package
    ├── settings.py # setting/configuration
    ├── urls.py # the url declarations for this django project; a "table of contents" of my django-powered site
    └── wsgi.py
```
